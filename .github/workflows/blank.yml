name: cipipeline
on: 
  push: 
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
         - uses: actions/checkout@v4
         - name: print hello
           run: echo "hello world"
test:
  runs-on: ubuntu-latest
  steps:
  - run: npm test
